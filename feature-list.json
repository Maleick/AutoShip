{
  "features": [
    {
      "id": "ci-workflow-rationalization",
      "name": "CI workflow rationalization",
      "status": "complete",
      "acceptance_tests": [
        "python3 -m unittest tests.test_workflow_contract -v",
        "python3 -m unittest tests.test_agent_ready_workflow -v",
        "python3 scripts/sync_wiki.py --check",
        "cargo clippy --all-targets --all-features -- -D warnings",
        "cargo test --all --all-features"
      ],
      "notes": "Completed pruning and simplification work, then reopened on 2026-04-14 to remove the redundant merged-branch cleanup workflow, collapse CI into one merge gate, switch agent-ready linked-PR detection from failing GraphQL to REST timeline reads, and repair the fmt/clippy drift that was leaving master red after PR #1600."
    },
    {
      "id": "m10-economy-execution-scope",
      "name": "M10 Economy execution scope",
      "status": "in_progress",
      "acceptance_tests": [
        "Roadmap-facing docs route provider-backed Soul/LLM work to M11 and economy execution loops to M10"
      ],
      "notes": "Loot intake, vendor cycle, banking cycle, and economy TUI summaries. Soul/LLM work routed to M11."
    },
    {
      "id": "textquest-no-run-compile-repair-20260417",
      "name": "TextQuest no-run compile repair",
      "status": "complete",
      "acceptance_tests": [
        "cargo test -p textquest --no-run"
      ],
      "notes": "Session compile-repair pass reduced `cargo test -p textquest --no-run` errors from 167 to 0 and restored missing orchestrator/config/runtime surfaces needed for test compilation."
    },
    {
      "id": "issue-1733-textquest-admin-backup-commands",
      "name": "textquest-admin backup commands",
      "status": "blocked",
      "acceptance_tests": [
        "cargo fmt --all --check",
        "cargo test -p textquest",
        "python3 scripts/dev-preflight.py"
      ],
      "notes": "Blocked on missing prerequisite work from issues #1729 and #1730. This branch has no `textquest-admin` binary, no `textquest/src/admin/` module, and no admin backup API/client/models to wire. Issue #1733 is marked `agent:ready`, but the required scaffolding is not present in `origin/master` or this worktree."
    }
  ]
}
