# Rust build artifacts
target/
/logs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store


# Added by cargo

/target

# Credential databases (sensitive — never commit)
credentials.db*

# Playwright MCP
.playwright-mcp/

# Local Claude workspace artifacts (runtime, not config)
.claude/worktrees/
logs/
autoresearch-launch.json
autoresearch-state.json
autoresearch-state.prev.json
research-results.tsv
autoresearch-project-sync.json
__pycache__/
*.pyc

# OpenWolf (all state, config, hooks, logs)
.wolf/

# Claude Code (all local config, hooks, rules, settings)
.claude/

# Local reference repos
mq2-definitions/
mq2-eqlib/
mq2-reference/

# Account credentials
accounts.csv
config/accounts.toml

# Large map text files (200MB+ each, use config/maps/*.zip instead)
config/maps/*.txt
config/accounts.csv

# Ghidra exports
data/ghidra-export/
