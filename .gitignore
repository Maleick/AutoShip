# Rust build artifacts
target/
/logs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store


# Added by cargo

/target

# Credential databases (sensitive — never commit)
credentials.db*

# Playwright MCP
.playwright-mcp/

# Local Claude workspace artifacts (runtime, not config)
.claude/worktrees/
logs/
autoresearch-*.json
autoresearch-*.tsv
autoresearch-project-sync.json
research-results.tsv
__pycache__/
*.pyc

# AutoShip / Beacon agent artifacts — never commit these into feature branches
AUTOSHIP_RESULT.md
AUTOSHIP_PROMPT.md
BEACON_RESULT.md
BEACON_PROMPT.md
*.master
.autoship/
.beacon/
autoresearch-security/
pane.log

# Root-local scratch/build leftovers
.watcher.pid
libobservability.rlib
test_zoning_module.rs

# Agent and OpenWolf runtime state
.wolf/

# Local Claude session config (personal, not shared)
.claude.local.md

# AI/tooling runtime state (keep out of repo)
.claude/
.aidesigner/
.serena/
.codex/
.codex-worktrees/
.codex/worktrees/

# Local-sync only (synced between machines, not in repo)
knowledge/
downloads/
HANDOFF.md
scripts/sync-to-frostreaver.sh

# Local reference repos
mq2-definitions/
mq2-eqlib/
mq2-reference/

# Local raw research harvests and cloned reference caches
/research/

# TUI runtime preferences (user-specific)
config/tui-prefs.toml

# Account credentials
accounts.csv
config/accounts.toml
config/.credentials
scripts/accounts_*.txt
!scripts/accounts_*.txt.example

# Large map text files (200MB+ each, use config/maps/*.zip instead)
config/maps/*.txt
config/accounts.csv

# Local runtime state and operator-managed caches. Canonical immutable Ghidra evidence lives in TextQuest-Ghidra.
data/
CLAUDE.md
